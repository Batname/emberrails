# json.array!(Product.all) do |product|
#   json.extract! product, :id, :title, :price, :description, :image, :comments
# end
# json.array!(Comment.all) do |product|
#   json.extract! product, :id, :body, :product_id
# end
